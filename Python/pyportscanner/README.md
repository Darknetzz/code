# pyportscanner

> **Canonical implementation:** Use the Rust port [`Rust/portscan`](../../Rust/portscan/) for release binaries and faster concurrent scanning.

TCP port scanner CLI. See `pyportscanner.py` for the legacy Python implementation.

```powershell
python pyportscanner.py 192.168.1.1 -p 80,443
```
