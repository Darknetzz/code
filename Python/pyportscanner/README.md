# pyportscanner

> **Canonical implementation:** Use [`Rust/rust-portscan`](../../Rust/rust-portscan/) (`rust-portscan` CLI) for release binaries and faster concurrent scanning.

TCP port scanner CLI. See `pyportscanner.py` for the legacy Python implementation.

```powershell
python pyportscanner.py 192.168.1.1 -p 80,443
```
