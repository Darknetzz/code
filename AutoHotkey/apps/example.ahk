; Per-application hotkeys — use #HotIf so bindings only apply in matching windows.
; Duplicate this file for each app and #Include from main.ahk

; #HotIf WinActive("ahk_exe Code.exe")
; ^!k::Send("^k")   ; example: only in VS Code / Cursor
; #HotIf
